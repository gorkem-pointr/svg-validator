# Copy to .env and fill in real values. .env is gitignored.
# Credentials for pulling the private image from Docker Hub.

DOCKER_REGISTRY_SERVER=docker.io
DOCKER_USERNAME=pointr
DOCKER_PASSWORD=
DOCKER_EMAIL=docker@pointr.tech
